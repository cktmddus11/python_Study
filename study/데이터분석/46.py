# 1. 웹 페이지의 html 가져옴
# 2. html 에서 필요한 데이터만 가져와서 파일(ex : csv)로 저장
# == 크롤링 ===
# 3. 파일 읽어서 그래프로 그릴 수 있도록 데이터 가공 == 전처리 == 
# 4. 데이터를 그래프로 그림 == 시각화 == 

# 웹 페이지 크롤링
# weather파일 코드는 
# 특정 태그가 있는지, 몇번째에 위치한 태그를 
# 가져온다든해서 체계적이지 못함, 깔끔하지 않고, 
# HTML이 변하면 코드를 다르게 수정해주어야되고 별로임

# 데이터를 csv 파일에 저장하기
